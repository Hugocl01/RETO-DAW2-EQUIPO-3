import Inscribirse from "../components/FormularioInscripcion/Inscribirse.jsx";

/**
 * Página `InscribirsePage` que muestra el formulario de inscripción.
 * 
 * @component
 * @returns {JSX.Element} - Elemento JSX que representa la página de inscripción.
 */
function InscribirsePage() {
    return (
        <>
            <title>Formulario de inscripción</title>
            <Inscribirse />
        </>
    );
}

export default InscribirsePage;
